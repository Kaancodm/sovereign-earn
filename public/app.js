const status=document.querySelector('#statusText');document.querySelector('#start').addEventListener('click',()=>status.textContent='Bereit – Authentifizierung wird als nächster Schritt verbunden.');document.querySelector('#login').addEventListener('click',()=>status.textContent='Firebase Auth ist für die nächste Integrationsstufe vorbereitet.');document.querySelector('#learn').addEventListener('click',()=>document.querySelector('.grid').scrollIntoView({behavior:'smooth'}));if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});
